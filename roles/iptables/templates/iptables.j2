# Generated by ansible
*filter
:INPUT ACCEPT [0:0]
:FORWARD ACCEPT [0:0]
:OUTPUT ACCEPT [152:11623]
:RH-Firewall-1-INPUT - [0:0]
-A INPUT -p tcp -m multiport ! --dports {{iptables_allowed_blacklist_ports}} -m set --match-set blacklist src -j DROP
-A INPUT -i lo -j ACCEPT
-A INPUT -j RH-Firewall-1-INPUT
-A FORWARD -j RH-Firewall-1-INPUT
-A RH-Firewall-1-INPUT -i lo -j ACCEPT
-A RH-Firewall-1-INPUT -p icmp -m icmp --icmp-type any -j ACCEPT
-A RH-Firewall-1-INPUT -m state --state RELATED,ESTABLISHED -j ACCEPT


{% for port in iptables_allowed_tcp_ports %}
-A INPUT -p tcp -m tcp --dport {{ port }} -j ACCEPT
{% endfor %}

{% for port in iptables_allowed_udp_ports %}
-A INPUT -p udp -m udp --dport {{ port }} -j ACCEPT
{% endfor %}

# Accept icmp ping requests.
# -A INPUT -p icmp -j ACCEPT

# Allow NTP traffic for time synchronization.
-A INPUT -p udp --sport 123 -j ACCEPT

# Additional custom rules.
{% for rule in iptables_additional_rules %}
{{ rule }}
{% endfor %}

# Log EVERYTHING (ONLY for Debug).
-A INPUT -j LOG

# Log other incoming requests (all of which are dropped) at 15/minute max.
-A INPUT -m limit --limit 15/minute -j LOG --log-prefix "iptables Dropped: "

# Drop all other traffic.
-A INPUT -j DROP
COMMIT

